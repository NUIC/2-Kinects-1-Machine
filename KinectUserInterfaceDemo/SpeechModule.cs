using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Speech.Recognition;

namespace KinectMenu
{
    class SpeechModule
    {
        //private Choices options = new Choices();

        //public delegate void SpeechEvent(String command);

        //public SpeechEvent SpeechRecognized;
        //public SpeechEvent SpeechRejected;

        //public SpeechModule(MainWindow window, List<String> choices)
        //{
        //    Regist
        //erRecognizedEvent(window.SpeechRecognized);
        //    RegisterRecognizedEvent(window.SpeechRejected);

        //    foreach (String choice in choices)
        //    {
        //        options.Add(choice);
        //    }

        //    GrammarBuilder builder = new GrammarBuilder();
        //    builder.Append(options);

        //    Grammar grammar = new Grammar(builder);


        //}

        //public void RegisterRecognizedEvent(SpeechEvent handler)
        //{
        //    SpeechRecognized += handler;
        //}

        //public void RegisterRejectedEvent(SpeechEvent handler)
        //{
        //    SpeechRejected += handler;
        //}

    }
}
